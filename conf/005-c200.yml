sources:
  uhttpd:
    name: uhttpd
    categories:
      Networks:
        name: Networks
        functions:
          g_socket_receive:
            name: uh_tcp_recv
            symbols: [uh_tcp_recv]
            synopsis: int32_t uh_tcp_recv(int32_t* arg1, int32_t buf, int32_t count)
            enabled: true
            par_cnt: i == 3
            par_dataflow: 'False'
            par_slice: i == 1 or i == 2